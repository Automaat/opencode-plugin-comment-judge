{
  "file": "timeparse/duration.py",
  "comment": "\"\"\"Parse a duration such as \"1h30m\" or \"45s\" into seconds.\n\nUnits are h, m, s and ms. Raises ValueError for an empty string or an unknown unit.\n\"\"\"",
  "context": "_UNIT_SECONDS = {\"h\": 3600, \"m\": 60, \"s\": 1, \"ms\": 0.001}\n\n\ndef parse_duration(text: str) -> float:\n    \"\"\"Parse a duration such as \"1h30m\" or \"45s\" into seconds.\n\n    Units are h, m, s and ms. Raises ValueError for an empty string or an unknown unit.\n    \"\"\"\n    parts = _DURATION.findall(text)\n    if not parts:\n        raise ValueError(f\"not a duration: {text!r}\")\n    return sum(float(amount) * _UNIT_SECONDS[unit] for amount, unit in parts)",
  "expected": "keep",
  "why": "Public API documentation: accepted input, units and errors."
}
